export enum ModalType {
  Note = 'note',
  List = 'list',
  Tag = 'tag',
  Themes = 'themes',
  Shortcuts = 'shortcuts',
  About = 'about',
  Account = 'account',
}

export enum ContextMenuType {
  Tags = 'tags',
  Background = 'background',
  Settings = 'settings',
  Account = 'account',
}
