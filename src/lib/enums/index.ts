export enum FormType {
  Note = 'note',
  TaskList = 'taskList',
  None = 'none',
}

export enum ModalType {
  Note = 'note',
  TaskList = 'taskList',
}

export enum ContextMenuType {
  Tags = 'tags',
  Brush = 'brush',
}
